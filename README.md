# Travel Planner API

RESTful API для планування подорожей та управління місцями для відвідування. Проект використовує FastAPI, SQLAlchemy та інтегрується з Art Institute of Chicago API для отримання інформації про місця.

## 📋 Зміст

- [Вимоги](#вимоги)
- [Швидкий старт](#швидкий-старт)
- [Встановлення](#встановлення)
- [Запуск](#запуск)
- [Docker](#docker)
- [API Endpoints](#api-endpoints)
- [Приклади використання](#приклади-використання)
- [Структура проекту](#структура-проекту)
- [Архітектура](#архітектура)
- [Валідації та обмеження](#валідації-та-обмеження)
- [База даних](#база-даних)
- [Інтеграція з ArtIC API](#інтеграція-з-artic-api)
- [Технології](#технології)

## Вимоги

- **Python 3.10+** (для локального запуску)
- **Docker** та **Docker Compose** (для Docker запуску)
- **pip** (для локального встановлення)

## Швидкий старт

### Запуск через Docker (рекомендовано)

```bash
# Клонуйте репозиторій
git clone <repository-url>
cd test_task_crewred

# Запустіть через Docker Compose
docker-compose up --build

# API буде доступне за адресою: http://localhost:8000
# Документація: http://localhost:8000/docs
```

### Перший запит (приклад)

**⚠️ Важливо:** `external_id` має бути **числовим ID** з ArtIC API, наприклад `27992`, `28560`, `25853` (не `"1"` або `"string"`!)

```bash
curl -X POST "http://localhost:8000/projects" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "My Art Tour",
    "places": [
      {"external_id": "27992", "notes": "Must see"}
    ]
  }'
```

**Валідні ID для тестування:** `27992`, `28560`, `25853`, `16568`, `20684`

### Локальний запуск

```bash
# Встановіть залежності
python -m pip install -r requirements.txt

# Запустіть сервер
uvicorn app.main:app --reload
```

## Встановлення

### Локальне встановлення

1. **Клонуйте репозиторій:**
```bash
git clone <repository-url>
cd test_task_crewred
```

2. **Створіть віртуальне середовище (рекомендовано):**
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

3. **Встановіть залежності:**
```bash
pip install -r requirements.txt
```

**Примітка:** Якщо `pip` не знайдено, використовуйте `python -m pip` або `python3 -m pip`

### Docker встановлення

Переконайтеся, що у вас встановлено Docker та Docker Compose:
- [Docker Desktop](https://www.docker.com/products/docker-desktop) (Windows/Mac)
- Або Docker Engine + Docker Compose (Linux)

## Запуск

### Локальний запуск

```bash
uvicorn app.main:app --reload
```

API буде доступне за адресою: `http://localhost:8000`

### Docker запуск

```bash
# Запуск у фоновому режимі
docker-compose up -d

# Перегляд логів
docker-compose logs -f

# Зупинка
docker-compose down

# Зупинка з видаленням volumes (очистить базу даних)
docker-compose down -v
```

## Docker

### Dockerfile

Проект включає оптимізований Dockerfile на базі Python 3.11-slim:
- Мінімальний розмір образу
- Кешування залежностей
- Правильна структура робочої директорії

### Docker Compose

Файл `docker-compose.yml` налаштований для:
- **Hot reload** - зміни в коді автоматично застосовуються
- **Персистентність даних** - база даних зберігається в `./data`
- **Health checks** - автоматична перевірка стану сервісу
- **Автоматичний перезапуск** при збоях

### Docker команди

```bash
# Збірка образу
docker-compose build

# Запуск
docker-compose up

# Запуск у фоновому режимі
docker-compose up -d

# Перегляд логів
docker-compose logs -f api

# Зупинка
docker-compose stop

# Повне видалення (включно з volumes)
docker-compose down -v

# Перебудова після змін
docker-compose up --build
```

## Документація API

Після запуску сервера доступна автоматична інтерактивна документація:

- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`
- **OpenAPI Schema**: `http://localhost:8000/openapi.json`

## API Endpoints

### Health Check
- **`GET /health`** - Перевірка стану сервісу
  - Повертає: `{"status": "ok"}`

### Projects (Проекти)

- **`POST /projects`** - Створити проект з місцями
  - **Обов'язково**: мінімум 1 місце в `places`
  - Body: `ProjectCreate` (name, description, start_date, places[])
  - Повертає: `ProjectDetailOut` (201)

- **`GET /projects`** - Список проектів
  - Query params: `limit` (default: 20, max: 100), `offset` (default: 0)
  - Повертає: `list[ProjectOut]` (200)

- **`GET /projects/{project_id}`** - Отримати проект з місцями
  - Повертає: `ProjectDetailOut` (200)
  - Помилка: 404 якщо проект не знайдено

- **`PATCH /projects/{project_id}`** - Оновити проект
  - Body: `ProjectUpdate` (name?, description?, start_date?)
  - Повертає: `ProjectDetailOut` (200)
  - Помилка: 404 якщо проект не знайдено

- **`DELETE /projects/{project_id}`** - Видалити проект
  - Повертає: 204 (No Content)
  - Помилка: 404 якщо проект не знайдено
  - Помилка: 409 якщо є відвідані місця

### Places (Місця)

- **`POST /projects/{project_id}/places`** - Додати місце до проекту
  - Body: `PlaceCreate` (external_id, notes?)
  - Повертає: `PlaceOut` (201)
  - Помилка: 404 якщо проект не знайдено або місце не існує в ArtIC API
  - Помилка: 409 якщо місце вже існує або досягнуто ліміт (10 місць)

- **`GET /projects/{project_id}/places`** - Список місць проекту
  - Query params: `limit` (default: 50, max: 100), `offset` (default: 0)
  - Повертає: `list[PlaceOut]` (200)
  - Помилка: 404 якщо проект не знайдено

- **`GET /projects/{project_id}/places/{place_id}`** - Отримати місце
  - Повертає: `PlaceOut` (200)
  - Помилка: 404 якщо проект або місце не знайдено

- **`PATCH /projects/{project_id}/places/{place_id}`** - Оновити місце
  - Body: `PlaceUpdate` (notes?, visited?)
  - При `visited: true` автоматично встановлюється `visited_at`
  - Автоматично оновлює `completed` статус проекту
  - Повертає: `PlaceOut` (200)
  - Помилка: 404 якщо проект або місце не знайдено

## Приклади використання

### Створити проект з місцями

**⚠️ Важливо:** `external_id` має бути **числом** (наприклад, `27992`), а не текстом (не `"1"` або `"string"`)!

```bash
curl -X POST "http://localhost:8000/projects" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Chicago Art Tour",
    "description": "Exploring art museums in Chicago",
    "start_date": "2024-06-01",
    "places": [
      {
        "external_id": "27992",
        "notes": "Must see - famous painting"
      },
      {
        "external_id": "28560",
        "notes": "Check opening hours before visit"
      }
    ]
  }'
```

**Примітка:** Якщо отримуєте помилку 404 "Place not found", перевірте, що `external_id` є валідним числовим ID з ArtIC API. Див. розділ "Як знайти валідні external_id" нижче.

**Відповідь:**
```json
{
  "id": 1,
  "name": "Chicago Art Tour",
  "description": "Exploring art museums in Chicago",
  "start_date": "2024-06-01",
  "completed": false,
  "places": [
    {
      "id": 1,
      "external_id": "27992",
      "title": "A Sunday on La Grande Jatte",
      "notes": "Must see - famous painting",
      "visited": false,
      "visited_at": null
    },
    {
      "id": 2,
      "external_id": "28560",
      "title": "The Bedroom",
      "notes": "Check opening hours before visit",
      "visited": false,
      "visited_at": null
    }
  ]
}
```

### Отримати список проектів

```bash
curl "http://localhost:8000/projects?limit=10&offset=0"
```

### Отримати проект з місцями

```bash
curl "http://localhost:8000/projects/1"
```

### Додати місце до проекту

**⚠️ Важливо:** `external_id` має бути **числом** (наприклад, `27992`), а не текстом!

```bash
curl -X POST "http://localhost:8000/projects/1/places" \
  -H "Content-Type: application/json" \
  -d '{
    "external_id": "27992",
    "notes": "Beautiful painting, need to see it"
  }'
```

**Валідні приклади external_id:** `27992`, `28560`, `25853`, `16568`, `20684`

### Оновити місце (позначити як відвідане)

```bash
curl -X PATCH "http://localhost:8000/projects/1/places/1" \
  -H "Content-Type: application/json" \
  -d '{
    "visited": true,
    "notes": "Visited on 2024-06-15. Amazing!"
  }'
```

### Оновити проект

```bash
curl -X PATCH "http://localhost:8000/projects/1" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Updated Chicago Art Tour",
    "description": "Updated description"
  }'
```

### Видалити проект

```bash
curl -X DELETE "http://localhost:8000/projects/1"
```

**Помилка якщо є відвідані місця:**
```json
{
  "detail": "Cannot delete project with visited places"
}
```

## Структура проекту

```
test_task_crewred/
├── app/
│   ├── __init__.py
│   ├── main.py              # Точка входу FastAPI
│   ├── core/
│   │   ├── __init__.py
│   │   └── db.py            # Конфігурація бази даних
│   ├── deps/
│   │   ├── __init__.py
│   │   └── db.py            # Dependency для DB сесій
│   ├── models/              # SQLAlchemy моделі
│   │   ├── __init__.py
│   │   ├── project.py       # Модель Project
│   │   └── place.py         # Модель ProjectPlace
│   ├── schemas/             # Pydantic схеми
│   │   ├── __init__.py
│   │   ├── project.py       # ProjectCreate, ProjectUpdate, ProjectOut
│   │   └── place.py          # PlaceCreate, PlaceUpdate, PlaceOut
│   ├── crud/                # CRUD операції
│   │   ├── __init__.py
│   │   ├── project.py       # CRUD для Project
│   │   └── place.py         # CRUD для ProjectPlace
│   ├── services/            # Бізнес-логіка
│   │   ├── __init__.py
│   │   ├── artic_service.py # Інтеграція з ArtIC API
│   │   └── project_service.py # Бізнес-логіка проектів
│   └── routes/              # HTTP endpoints
│       ├── __init__.py
│       ├── health.py        # Health check endpoint
│       ├── projects.py      # CRUD для проектів
│       └── places.py        # CRUD для місць
├── data/                    # База даних (створюється автоматично)
├── Dockerfile               # Docker образ
├── docker-compose.yml      # Docker Compose конфігурація
├── .dockerignore           # Файли для ігнорування в Docker
├── .gitignore              # Файли для ігнорування в Git
├── requirements.txt        # Python залежності
└── README.md               # Документація
```

## Архітектура

Проект побудований за принципами **Clean Architecture** та **Separation of Concerns**:

1. **Models** (`app/models/`) - SQLAlchemy моделі для роботи з БД
2. **Schemas** (`app/schemas/`) - Pydantic схеми для валідації та серіалізації
3. **CRUD** (`app/crud/`) - Тонкий шар для базових операцій з БД
4. **Services** (`app/services/`) - Бізнес-логіка та інтеграції
5. **Routes** (`app/routes/`) - HTTP endpoints, тільки маршрутизація та валідація

### Потік даних

```
HTTP Request → Route → Service → CRUD → Model → Database
                ↓
            Schema Validation
                ↓
HTTP Response ← Route ← Service ← CRUD ← Model ← Database
```

## Валідації та обмеження

### Проекти

- ✅ **Мінімум 1 місце, максимум 10 місць** на проект
- ✅ **Неможливо видалити проект**, якщо хоча б одне місце позначене як відвідане
- ✅ **Автоматичне позначення проекту як завершеного**, коли всі місця відвідані
- ✅ **Валідація полів**: name (1-200 символів), start_date (формат дати)

### Місця

- ✅ **Валідація існування** місця в Art Institute of Chicago API перед додаванням
- ✅ **Унікальність external_id** в межах одного проекту (UniqueConstraint)
- ✅ **Заборона дублікатів** external_id в одному запиті створення
- ✅ **Автоматичне встановлення visited_at** при позначенні як відвідане
- ✅ **Автоматичне очищення visited_at** при знятті позначки відвіданого

### HTTP Статус коди

- `200 OK` - Успішний запит
- `201 Created` - Ресурс успішно створено
- `204 No Content` - Ресурс успішно видалено
- `404 Not Found` - Ресурс не знайдено
- `409 Conflict` - Конфлікт (місце вже існує, досягнуто ліміт, неможливо видалити)
- `422 Unprocessable Entity` - Помилка валідації даних

## База даних

### SQLite

Проект використовує **SQLite** для простоти та швидкого старту:
- База даних створюється автоматично при першому запуску
- Файл: `app.db` (локально) або `./data/app.db` (Docker)
- Міграції не потрібні - таблиці створюються автоматично через SQLAlchemy

### Структура таблиць

**projects**
- `id` (INTEGER PRIMARY KEY)
- `name` (VARCHAR(200) NOT NULL)
- `description` (TEXT NULL)
- `start_date` (DATE NULL)
- `completed` (BOOLEAN DEFAULT FALSE)

**project_places**
- `id` (INTEGER PRIMARY KEY)
- `project_id` (INTEGER FOREIGN KEY → projects.id)
- `external_id` (VARCHAR(64) NOT NULL)
- `title` (VARCHAR(300) NULL)
- `notes` (TEXT NULL)
- `visited` (BOOLEAN DEFAULT FALSE)
- `visited_at` (DATETIME NULL)
- UNIQUE CONSTRAINT: `(project_id, external_id)`

### Резервне копіювання

Для Docker: база даних зберігається в `./data/app.db` і персистентна між перезапусками.

## Інтеграція з ArtIC API

Проект інтегрується з [Art Institute of Chicago API](https://api.artic.edu/docs/):

### Використовуваний endpoint

- **`GET /api/v1/artworks/{id}`** - Отримання інформації про конкретний артефакт

### Логіка інтеграції

1. При додаванні місця до проекту виконується запит до ArtIC API
2. Перевіряється існування артефакту з вказаним `external_id`
3. Якщо артефакт існує - зберігається `external_id` та `title`
4. Якщо артефакт не знайдено - повертається помилка 404

### Як знайти валідні external_id

**Важливо:** `external_id` має бути **числовим ID** артефакту з ArtIC API, а не текстом!

**Способи знайти валідні ID:**

1. **Через API пошук:**
   ```bash
   curl "https://api.artic.edu/api/v1/artworks/search?q=monet&limit=5"
   ```
   У відповіді буде поле `id` для кожного артефакту.

2. **Приклади валідних ID для тестування:**
   - `27992` - "A Sunday on La Grande Jatte" (Seurat)
   - `28560` - "The Bedroom" (Van Gogh)
   - `25853` - "Water Lilies" (Monet)
   - `16568` - "The Great Wave" (Hokusai)
   - `20684` - "Nighthawks" (Hopper)

3. **Перевірка валідності ID:**
   ```bash
   curl "https://api.artic.edu/api/v1/artworks/27992"
   ```
   Якщо отримуєте дані - ID валідний. Якщо 404 - ID не існує.

### Обробка помилок

- **200** - Артефакт знайдено, дані зберігаються
- **404** - Артефакт не знайдено, повертається помилка клієнту
- **Інші статуси** - Викликається `raise_for_status()` для обробки помилок

## Технології

### Backend Framework
- **FastAPI** 0.104+ - сучасний, швидкий веб-фреймворк з автоматичною документацією

### Database
- **SQLAlchemy** 2.0+ - потужний ORM з підтримкою типізації
- **SQLite** - легка база даних для розробки

### Validation & Serialization
- **Pydantic** 2.0+ - валідація даних та серіалізація

### HTTP Client
- **httpx** 0.25+ - асинхронний HTTP клієнт для зовнішніх API

### Development
- **uvicorn** - ASGI сервер для FastAPI
- **Docker** - контейнеризація
- **Docker Compose** - оркестрація контейнерів

## Розробка

### Локальна розробка

1. Створіть віртуальне середовище
2. Встановіть залежності: `pip install -r requirements.txt`
3. Запустіть з reload: `uvicorn app.main:app --reload`
4. Код автоматично перезавантажується при змінах

### Docker розробка

1. Запустіть: `docker-compose up`
2. Код монтується як volume, зміни застосовуються автоматично
3. Логи: `docker-compose logs -f api`

### Тестування API

Використовуйте Swagger UI (`http://localhost:8000/docs`) для інтерактивного тестування або curl/Postman для автоматизації.

## Troubleshooting

### Проблема: База даних не створюється

**Рішення:** Переконайтеся, що директорія має права на запис. Для Docker перевірте volumes в `docker-compose.yml`.

### Проблема: Помилка при додаванні місця (404)

**Рішення:** Перевірте, що `external_id` існує в ArtIC API. Використовуйте валідні ID (наприклад, 27992, 28560).

### Проблема: Docker не запускається

**Рішення:**
- Перевірте, що Docker запущений
- Перевірте порт 8000 на вільність: `netstat -an | grep 8000`
- Перебудова: `docker-compose up --build`

### Проблема: Помилка "Project must have at least 1 place"

**Рішення:** При створенні проекту обов'язково передайте масив `places` з мінімум одним елементом.

## Ліцензія

Цей проект створено для тестового завдання.

## Контакти

Для питань та зворотного зв'язку звертайтеся до рекрутера.
